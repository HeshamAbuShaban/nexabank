<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/_16sdp"
    tools:context=".ui.fragments.LoanFragment">
    <!-- Loan Overview -->
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/tvLoanOverview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/_14sdp"
        android:layout_marginBottom="@dimen/_8sdp"
        android:textSize="@dimen/_18ssp"
        android:textStyle="bold"
        tools:text="Loan Overview" />

    <!-- Recycler Item -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/_8sdp"
        app:cardCornerRadius="@dimen/_8sdp"
        app:cardElevation="@dimen/_4sdp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/_16sdp">

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/tvLoanAmount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/black"
                android:textSize="16sp"
                tools:text="Loan Amount: $5000" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/tvRepaymentDate"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                tools:text="Next Repayment Date: Jan 15, 2025" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Floating Action Button for New Loan -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabNewLoan"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:layout_margin="@dimen/_16sdp"
        android:contentDescription="@string/add_new_loan"
        app:srcCompat="@drawable/ic_add" />
</LinearLayout>