<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="ActiveIndicator" parent="ShapeAppearance.Material3.NavigationBarView.ActiveIndicator">
        <item name="backgroundTint">@color/transparent</item>
    </style>

    <style name="AppTextStyle" parent="ThemeOverlay.Base.Theme.NexaBank.HighContrast">
        <item name="android:textViewStyle">@style/AppTextStyle</item>
        <item name="backgroundTint">@color/transparent</item>
    </style>

    <style name="MusicSubtitleText" parent="Widget.MaterialComponents.TextView">
        <!--
                android:ellipsize="marquee"
                android:marqueeRepeatLimit="marquee_forever"
                android:maxLength="25"
                android:maxLines="1"
        -->
        <!--        <item name="android:ellipsize">end</item>-->
        <item name="android:ellipsize">marquee</item>
        <item name="android:marqueeRepeatLimit">marquee_forever</item>
        <item name="android:scrollHorizontally">true</item>
        <item name="android:maxLines">1</item>

    </style>


    <style name="SelectableItemBackgroundStyle">
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <style name="SelectableItemBackgroundBorderLessStyle">
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

</resources>