<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.NexaBank" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="fontFamily">@font/cairo_medium</item>
        <!--Imp-->
        <item name="android:windowBackground">@color/nice_gray</item>
        <item name="android:navigationBarColor">@color/nice_gray</item>
        <!--Imp-->
        <!--AppTStyle-->
        <item name="android:textViewStyle">@style/AppTextViewStyle</item>
        <!-- enable window content transitions -->
        <item name="android:windowActivityTransitions">true</item>

    </style>

    <style name="Theme.NexaBank" parent="Base.Theme.NexaBank" />

    <style name="AppTextViewStyle" parent="android:Widget.TextView">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>